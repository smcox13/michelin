services:
  tirelens:
    build:
      context: .
    image: tirelens:latest
    init: true
    ports:
      - "8501:8501"
    restart: unless-stopped
    environment:
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      OPENAI_MODEL: ${OPENAI_MODEL:-gpt-4.1-mini}
